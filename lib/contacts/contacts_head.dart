import 'package:flutter/material.dart';
import 'contacts_header.dart';

class ContactsHeads extends StatelessWidget {
  const ContactsHeads({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(),
    );
  }
}
